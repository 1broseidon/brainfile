{
  "name": "brainfile-monorepo",
  "private": true,
  "version": "0.18.1",
  "description": "Brainfile — markdown/YAML task boards for humans and agents: core library and CLI.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/1broseidon/brainfile"
  },
  "workspaces": [
    "core",
    "cli"
  ],
  "scripts": {
    "build": "npm run build -w core && npm run build -w cli",
    "test": "npm run test:ci -w core && npm test -w cli",
    "version:set": "node .github/scripts/sync-version.mjs"
  }
}
